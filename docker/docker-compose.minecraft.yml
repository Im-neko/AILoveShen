# Local Minecraft Java server for development (Mineflayer bot target)
# Usage: cd docker && docker compose -f docker-compose.minecraft.yml up -d
#
# - Paper 1.21.4 (matches minecraft.bot.version in config/default.yaml)
# - offline-mode so the Mineflayer bot can join without a Microsoft account.
#   Keep this server local; do not expose port 25565 to the internet.
# - Starting the server accepts the Minecraft EULA: https://aka.ms/MinecraftEULA
# - Every recipe is unlocked for the players (the bot) when they join: the bridge crafts through the
#   recipe book, which only fills the grid for unlocked recipes, while a player placing the items by
#   hand needs no unlock (vanilla unlocks the torch only with a stone pickaxe, the chest only with 10
#   slots filled). The unlocks are saved with the player, so a grant ~10s after joining lasts.

services:
  minecraft:
    image: itzg/minecraft-server:java21
    container_name: ailoveshen-minecraft
    ports:
      - "127.0.0.1:${MC_PORT:-25565}:25565"
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      VERSION: "1.21.4"
      MEMORY: "2G"
      ONLINE_MODE: "FALSE"
      DIFFICULTY: "easy"
      MODE: "survival"
      MOTD: "AILoveShen dev server"
      VIEW_DISTANCE: "8"
      SIMULATION_DISTANCE: "6"
      SPAWN_PROTECTION: "0"
      ENABLE_RCON: "TRUE"
      RCON_PASSWORD: "${MC_RCON_PASSWORD:-ailoveshen-dev}"
      RCON_CMDS_ON_CONNECT: "recipe give @a *"
    volumes:
      - minecraft-data:/data
    restart: unless-stopped
    stdin_open: true
    tty: true

volumes:
  minecraft-data:
